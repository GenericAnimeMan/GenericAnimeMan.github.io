$("document").ready( function() {
    header = `
        <h1>test header</h1>
    `
    $("#header").html(header);

    footer = `
        <p>don't steal this please i worked really hard on it (not really).</p>
    `
    $("#footer").html(footer);

})